import styled from 'styled-components';

export const Container = styled.header`
  width: 100%;
  max-width: 128rem;
  margin: 0 auto;
  padding: 3.5rem;

  .logo-img {
    margin-left: 10%;
    cursor: pointer;
  }
`